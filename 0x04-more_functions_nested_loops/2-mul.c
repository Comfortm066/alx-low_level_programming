#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: The first number
 * @b: The second number
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
